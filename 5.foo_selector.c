#include "main.h"


/**
 * @brief 
 * 
 */
void foo_selector(char *token, int line_nbr)
{

{
stack_t *stack = NULL;
int i = 0;

instruction_t opcode_list[] = {
	{"push", push},
    {"pall", pall},
	// {"pint", pint},
	// {"pop", pop},
	// {"pint", pint},
	// {"swap", swap},
	// {"nop", nop},
	// {"add", add},
	// {"sub", sub},
	// {"mul", mul},
	// {"div", _div},
	// {"mod", mod},
	// {"pchar", pchar},
	// {"pstr", pstr},
	// {"rotl", rotl},
	// {"rotr", rotr},
	// {"stack", stk},
	// {"queue", queue},
}

}